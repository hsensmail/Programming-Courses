// Create an array 'stringArr' that contains strings "a", "b", "c", and "d".
const stringArr = ["a", "b", "c", "d"];

// Create an array 'numArr' that contains numbers 1, 2, 3, 4, and 5.
const numArr = [1, 2, 3, 4, 5];

// Create an array 'boolArr' that contains boolean values: true and false.
const boolArr = [true, false];

// Create an array 'mixed' that contains different data types:
// "a" (string), 2 (number), true (boolean), undefined, null,
// an object with a property 'a' set to "a", and an array with a single string "b".
const mixed = ["a", 2, true, undefined, null, { a: "a" }, ["b"]];

// Use 'console.log' to print the entire 'mixed' array to the console.
console.log(mixed);
